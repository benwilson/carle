# Hardware observation logs live here.
#
# A command entry with status "confirmed" points at a log file in this directory, and
# tests/test_table_invariants.py fails if that path does not resolve. Empty until the
# first session with a real robot.
